 AUTH SYSTEM & ROLES
Use a single login page for Super Admin & Restaurant Admin.

No registration — only Super Admin can create Restaurant Admins.

JWT-based auth with role-based routing.

Store & refresh tokens securely.

Login inputs: email, password (fix LTR issue in RTL layout).

🧑‍💼 SUPER ADMIN PANEL
Sidebar: Dashboard, Add Restaurant.

Dashboard Page:

List: Restaurant name, slug link, admin email.

Actions (פעולות): View/reset credentials, edit, delete, view QR.

Add Restaurant form:

Fields: name, admin email/password.

Auto-create: slug, default colors, placeholder logo.

Generate QR and activate /menus/{slug}.

פעולות dropdown:

View/reset password.

Edit restaurant info (name, colors, etc).

Delete with confirmation.

Display & copy/download public link + QR.

🍽️ RESTAURANT ADMIN PANEL
Sidebar: Dashboard, Categories, Items, Menu Customization.

Highlight active tab with smooth UX.

Categories Page:

Add/Edit/Delete categories.

Upload/select icon.

Items Page:

Add/Edit/Delete items per category.

Fields: name, ₪ price, description, image upload.

Menu Customization:

Upload/change logo.

Instagram, Facebook, WhatsApp links.

Choose background + main button colors.

🌐 PUBLIC MENU PAGE /menus/{slug}
RTL layout.

Fully responsive (mobile-first).

Display:

Logo, categories (tabs/icons), grouped items.

Styled by restaurant’s custom colors.

Smooth transitions between categories.

QR + social/contact buttons.

Pull all content from DB (no hardcoded data).

🔒 ROLE-BASED PERMISSIONS
Super Admin: Full access.

Restaurant Admin: Access only to their restaurant data.

Strict route guarding (no cross-access).

🎨 DESIGN & UX
RTL enforced globally.

Tailwind CSS or styled-components.

Clean, modern, minimal UI.

Use modals, transitions, feedback, and loaders.

All forms have validation + success/fail feedback.

✅ FUNCTIONALITY CHECKLIST
All forms, buttons, links, sidebars work.

All data dynamic from DB.

Logo/color/social changes reflect live on public menu.

Protected routes per user role.

No broken UI, dead links, or bugs.

QR code + public link generation works.

🧪 DELIVERY CHECKLIST
✅ Super Admin login

✅ Add Restaurant fully working

✅ Sidebar navigation working

✅ All פעולות dropdown actions functional

✅ Restaurant Admin login

✅ Restaurant Admin sees only their content

✅ Categories & Items CRUD functional

✅ Logo/colors/social editable

✅ /menus/{slug} renders live DB content

✅ All buttons/pages/components work

🌱 SEEDER SCRIPT PROMPT (Node.js + Drizzle ORM)
Seeder file: drizzle/seed.ts

Use Drizzle’s insert(...).values(...) + uuidv4() for IDs.

SUPER ADMIN
Email: superadmin@example.com

Password: SuperSecure123 (bcrypt)

Role: superadmin

RESTAURANTS + ADMINS
Create 3 demo restaurants:

Falafel Express → slug: falafel-express

Pizza Bazaar → slug: pizza-bazaar

Sushi Delight → slug: sushi-delight

Each has:

Placeholder logo: https://via.placeholder.com/150

Unique background + button colors

Socials: Instagram, Facebook, WhatsApp

Admin Email: admin+{slug}@example.com

Admin Password: Admin1234 (bcrypt)

Role: admin

CATEGORIES & ITEMS
Each restaurant has:

2–3 Categories (e.g., “Main Dishes”, “Drinks”)

Each category: 2–3 items (name, price in ₪, optional image)

Example:

Shawarma Plate – ₪45 – image

Mint Lemonade – ₪15

🧠 SEEDING BEST PRACTICES
Use fixed or placeholder images.

Auto-link restaurant_id in users, categories, items.

Avoid duplicate slugs/emails.

Include .env.example with DB + JWT config.

Seed using pnpm seed or npm run seed.

✅ FINAL SEEDING CHECKLIST
Super Admin login works.

Restaurant Admins login & see only their content.

/menus/{slug} pages live with full data.

QR + share links work.

Color/logo customization visible.

No broken data or orphan records.

