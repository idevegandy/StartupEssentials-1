continuing to work on implementing the activity history feature for Super Admin users. Let's check what's currently in the super admin layout component to see how we should integrate our new page: 


Searched for "find components/layouts/super-admin.tsx fix check what still need mplement an activity log view for the Super Admin to track Resturnaut admins’ login timestamps and actions (such as edits to their info or menu)

I.

Fix admin permission isolation

Ensure that each Resturnaut admin can only view and manage their own data and features, without access to other admins or Super Admin activities.

Fix “נהל” (Edit) functionality

Debug and fix the "נהל" (Edit) button functionality for updating Resturnaut admin info, both in the main widget and the management panel.

🧭 Navigation & UI/UX
Fix Settings button in sidebar

Debug the Settings button in the sidebar menu — ensure it opens the settings panel/modal correctly.

Fix Language toggle feature

Repair the language switcher so that it properly changes the admin panel language across the UI.

Fix hidden text on mobile

Detect and resolve all hidden or cut-off texts in mobile view — especially buttons, labels, inputs, and tables.

Improve mobile UI/UX

Refactor CSS/styling and layout logic to improve UI and UX across mobile devices (responsive layout, touch interactions, spacing, font sizes).

🍽️ Menu & Restaurant Features
Improve menu page design (desktop & mobile)

Redesign the menu pages for both desktop and mobile with better layout, clearer sections, and user-friendly interaction patterns.

Fix NaN issue in item pricing

Ensure all item prices are properly handled and displayed in shekels (₪), and prevent “NaN” from appearing due to parsing or missing data.

Enable full category editing

Allow Resturnaut admins to edit category names, icons, and not just the menu items.

Add restaurant contact info to menu page

Display restaurant contact info (email, phone, location, etc.) in the menu page for users — fetched dynamically from the Resturnaut admin data.

Add contact info editing to admin panel

Add a feature inside the Resturnaut admin panel for editing their restaurant contact info (not just visible on client-side).

🧾 Storage & File Uploads
Check and fix image upload functionality

Test and ensure image upload features across all components (admins dashboard, profile, menus, items) are connected properly to app storage or database.